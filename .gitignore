# Không bao giờ đẩy secrets
.streamlit/secrets.toml
*.key
.env
*_SALT*
# Khoá API của trợ lý — chỉ nằm trong secrets.toml (máy) hoặc ô Secrets
# của Streamlit Cloud (web), không bao giờ trong repo

# Rác Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Rác Office / hệ điều hành
~$*
*.tmp
Thumbs.db
.DS_Store

# CSV cũ / bản sao lưu — chỉ đẩy đúng file đang dùng
data/*_backup*.csv
# mọi bản sao lưu trước khi sửa (truoc_bosung, truoc_thayKB, truoc_nap…)
data/*_truoc_*.csv
data/*_truoc_*.csv.gz
# bản CSV chưa nén — chỉ đẩy bản .csv.gz (76 MB xuống 6 MB, nội dung y hệt)
data/score_clean_full.csv
data/score_clean_2608_0109.csv
data/*_HONG_*.csv
data/full_score_clean_v*.csv

# TUYET DOI khong day file goc con so dien thoai that
*_GOC_CO_SDT.csv
*goc_truoc_andanh*
_goc_co_sdt/

# Ban xem truoc: co NHUNG KHOA API de chay that tren may.
# Tuyet doi khong commit.
F2DR_Van_Hanh_xemtruoc.html

# Thư mục app.py dựng lúc chạy để phục vụ dashboard qua HTTP.
# File trong đây đã ghép trợ lý nên CÓ KHOÁ API — không bao giờ commit.
static/
